/* eslint-disable no-undef */
/// <reference types="Cypress" />
/*
describe('Scrape', () => {
  it('Visits login', () => {
    cy.visit('http://localhost:3000');
    cy.findByText('Login').click();
    cy.findByText('Login details').should('exist');
  });
  it('Sign in as admin', () => {
    cy.findByLabelText('Username').type('admin');
    cy.findAllByLabelText('Password').type('test1');
    cy.get('#loginBtn').click();
    cy.findByText('Home').should('exist');
    cy.findByText('Scrape').should('exist');
  });
  it('Navigate to Scrape', () => {
    cy.findByText('Scrape').click();
    cy.get('h1').contains('Scrapes');
  });
  it('Log out', () => {
    cy.get('#logBtn').click();
    cy.get('#logoutBtn').click();
    cy.findByText('Jokes').should('not.exist');
  });
});
*/
